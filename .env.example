DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# 如果 DATABASE_URL 已存在，可在 Vercel 将 Neon 的 Custom Prefix 设为 NEON：
# NEON_URL=postgresql://user:password@host/database?sslmode=require
# 如果 Custom Prefix 保持为 STORAGE：
# STORAGE_URL=postgresql://user:password@host/database?sslmode=require
ADMIN_PASSWORD=Redbridge1982
SESSION_SECRET=replace-with-a-long-random-secret
